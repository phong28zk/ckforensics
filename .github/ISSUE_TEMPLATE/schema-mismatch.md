---
name: Schema mismatch (JSONL drift)
about: Parser fails or misclassifies events from a new Claude Code version
labels: schema-drift
---

**Claude Code version** (`claude --version` or release date)

**ckforensics version** (`ckforensics --version`)

**Sample line** that fails to parse (sanitize secrets first):
```json

```

**Error output** from `ingest`:
```

```

**What changed** in this CC version vs previous (if known)
